<% if current_user %>

var new_message = $("<%= j render 'layouts/user_messages',user: @message.user.email, message: @message.content, position: 'left' %>").hide();

$('.user-messages').last().append(new_message);
// new_message.fadeIn();

<% elsif current_artist %>
var new_message = $("<%= j render 'layouts/user_messages',user: @message.artist.email, message: @message.content, position: 'left' %>").hide();

$('.user-messages').last().append(new_message);
// new_message.fadeIn();
//new_message.slideDown();

<% else %>
<% end %>


